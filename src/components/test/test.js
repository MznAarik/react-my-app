 {/* {seriesData.map((curElem) =>

                <Card shadow="sm" padding="xs" radius="md" withBorder style={{ width: '400px', height: '450px', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
                    <Card.Section key={curElem.id}>
                        <Image
                            src={curElem.image_url}
                            height={200}
                            alt={curElem.name}
                        />
                    </Card.Section>

                    <Group justify="space-between" mt="md" mb="xs">
                        <Text fw={500}><strong>Name:</strong> {curElem.name} </Text>
                        <Badge color="pink"><strong>Rating:</strong> {curElem.rating}</Badge>
                    </Group>

                    <Text size="sm" c="dimmed">
                        <strong> Description:</strong> {curElem.description}
                    </Text>

                    <Text size="sm" c="dimmed">
                        <strong> Cast:</strong> {curElem.cast}
                    </Text>

                    <Text size="sm" c="dimmed">
                        <strong> Genre:</strong> {curElem.genre}
                    </Text>

                    <a href={curElem.watch_url} target='_blank'>
                        <Button fullWidth mt="md" radius="md" variant="gradient"
                            gradient={{ from: 'grape', to: 'cyan', deg: 226 }}>
                            Watch Now!
                        </Button>
                    </a>
                </Card >
            )};
 */}
